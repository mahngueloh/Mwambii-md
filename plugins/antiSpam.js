// Anti-Spam Plugin
module.exports = {
  name: 'antiSpam',
  description: 'Anti-spam functionality',
  async handle(message, client) {
    try {
      // Anti-spam logic
    } catch (error) {
      console.error('Error in antiSpam plugin:', error);
    }
  }
};
